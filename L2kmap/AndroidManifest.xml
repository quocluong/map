<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uit.l2kmap"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="uit.l2kmap.L2kmap"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="uit.l2kmap.LoadingSVGfile"
            android:label="@string/title_activity_loading_svgfile" >
        </activity>
        <activity
            android:name="uit.l2kmap.WifiScan"
            android:label="@string/title_activity_wifi_scan" >
        </activity>
        <activity
            android:name="uit.l2kmap.GetPosition"
            android:label="@string/title_activity_get_position" >
        </activity>
    </application>

</manifest>
